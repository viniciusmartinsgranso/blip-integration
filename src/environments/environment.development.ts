import { keys } from "./shared-environment/keys";
import { routes } from "./shared-environment/routes";

export const environment = {
  apiUrl: 'https://http.msging.net/commands',
  routes,
  keys,
};